# 🔐 Изменения в системе авторизации

## 📦 Дата внедрения: 27 января 2026

## 🎯 Что изменилось?

### Основное изменение
Приложение теперь имеет **полную защиту от внешнего доступа**:
- ✅ Доступ только через Telegram WebApp
- ✅ Таблица авторизованных пользователей создается автоматически
- ✅ Браузерный доступ полностью заблокирован

---

## 📝 Список измененных файлов

### Backend

#### 1. `backend/src/server.js`
**Что изменилось:**
- Добавлен импорт `fs` и `path` для чтения файлов миграций
- Обновлена функция `initializeDatabase()`:
  - Проверяет и создает поле `telegram_id` в таблице `users`
  - Автоматически выполняет миграцию из `05-add-telegram-auth.sql`
  - Проверяет наличие авторизованных пользователей
  - Выводит предупреждение если авторизованных пользователей нет

**Как использовать:**
- Никаких дополнительных действий, все работает автоматически
- Проверьте логи при запуске для подтверждения

#### 2. `backend/src/models/auth.js` (новый файл)
**Что это:**
- Middleware для проверки авторизации
- Проверяет что запрос из Telegram WebApp
- Автоматически создает запись в `users` при необходимости

**Основные функции:**
```javascript
authMiddleware - проверяет авторизацию перед всеми защищенными endpoints
isTelegramWebApp() - определяет что это Telegram WebApp запрос
```

#### 3. `backend/src/routes/auth.js` (переделан)
**Endpoints:**
- `GET /api/auth/check` - быстрая проверка авторизации
- `GET /api/auth/me` - информация о текущем пользователе
- Оба проверяют что это Telegram WebApp

**Что было удалено:**
- `POST /api/auth/register` - не нужен, проверка через таблицу БД

#### 4. `backend/src/routes/lists.js`
**Что изменилось:**
- Методы теперь используют `req.user.id` вместо hardcoded `1`
- Все списки связаны с текущим авторизованным пользователем

**Методы:**
- `GET /` - возвращает только списки текущего пользователя
- `POST /` - создает список для текущего пользователя

#### 5. `backend/src/routes/items.js`
**Что изменилось:**
- `POST /` - создает товар с `req.user.id`

---

### Frontend

#### 1. `frontend/services/apiService.ts` (обновлена)
**Новые методы:**
- `getTelegramData()` - получает данные из Telegram WebApp API
- `checkAuth()` - проверяет авторизацию с обновленной логикой
- `getCurrentUser()` - получает информацию о пользователе
- `logout()` - выход из системы

**Что изменилось в существующих методах:**
- `fetch()` - теперь рассылает `X-Telegram-Id` и `X-Telegram-WebApp` заголовки
- Улучшена обработка ошибок авторизации

#### 2. `frontend/App.tsx` (обновлена)
**Что изменилось:**
- Добавлена проверка авторизации при загрузке
- Новое состояние `isAuthorized` и `authError`
- Экран ошибки авторизации если доступ запрещен
- Кнопка выхода в header
- Улучшена обработка ошибок при операциях

---

### База данных

#### 1. `init-db/05-add-telegram-auth.sql` (переделана)
**Что создается:**
```sql
-- Таблица AUTHORIZED_USERS
- id (SERIAL PRIMARY KEY)
- telegram_id (BIGINT UNIQUE NOT NULL) ← Ключевое поле!
- username (VARCHAR)
- description (VARCHAR)
- is_active (BOOLEAN)
- authorized_at (TIMESTAMP)
- authorized_by (VARCHAR)

-- Индексы
- idx_authorized_users_telegram_id
- idx_authorized_users_active
```

**Начальные данные:**
- Одна запись с telegram_id = 123456789 (нужно обновить)

---

## 🔄 Миграция с предыдущей версии

### Автоматическое
При первом запуске бэкенда:
1. ✅ Проверяется наличие `telegram_id` в таблице `users`
2. ✅ Автоматически добавляется если отсутствует
3. ✅ Создается таблица `authorized_users` из SQL файла
4. ✅ Создаются индексы для быстрого поиска

### Ручное (если требуется)
```bash
# Подключиться к БД
docker exec -it procurement-db psql -U procurement_user -d procurement_db

# Добавить авторизованных пользователей
INSERT INTO authorized_users (telegram_id, username) 
VALUES (ваш_telegram_id, 'your_name');

# Убедиться что старые записи в users удалены (опционально)
DELETE FROM users WHERE username = 'test_user';
```

---

## 🚀 Как развернуть обновление

```bash
# 1. Остановить приложение
./stop-project.sh

# 2. Обновить код (git pull)
git pull

# 3. Пересобрать докер образы
./rebuild-project.sh

# 4. Запустить
./start-project.sh

# 5. Проверить логи
docker logs procurement-backend | tail -20

# 6. Добавить авторизованных пользователей
docker exec -it procurement-db psql -U procurement_user -d procurement_db
INSERT INTO authorized_users (telegram_id, username) VALUES (your_id, 'your_name');
```

---

## 🛡️ Что теперь защищено

### Блокируемые запросы:

| Тип запроса | Статус | Сообщение |
|-------------|--------|-----------|
| Из браузера | 403 | "Доступ только из Telegram" |
| Без Telegram ID | 401 | "Телеграм ID не передан" |
| ID не в БД | 403 | "Доступ запрещен. Telegram ID не авторизован" |
| Неактивный пользователь | 403 | "Доступ запрещен" |

### Разрешенные запросы:

✅ Из Telegram WebApp  
✅ С валидным `X-Telegram-Id` заголовком  
✅ С `X-Telegram-WebApp: true` заголовком  
✅ С active записью в таблице `authorized_users`  

---

## 📊 Примеры использования

### Добавить пользователя

```bash
docker exec -it procurement-db psql -U procurement_user -d procurement_db

INSERT INTO authorized_users (telegram_id, username, description) 
VALUES (123456789, 'ivan', 'Иван Петров');
```

### Просмотреть всех

```sql
SELECT * FROM authorized_users WHERE is_active = true;
```

### Запретить доступ

```sql
UPDATE authorized_users SET is_active = false 
WHERE telegram_id = 123456789;
```

### Удалить пользователя

```sql
DELETE FROM authorized_users WHERE telegram_id = 123456789;
```

---

## 🔍 Как проверить работает ли авторизация

### 1. Проверить логи бэкенда
```bash
docker logs procurement-backend | grep -E "(авторизац|ВНИМАНИЕ)"
```

### 2. Попытаться зайти из браузера
- Должно показать: "Доступ только из Telegram"

### 3. Проверить БД
```bash
docker exec -it procurement-db psql -U procurement_user -d procurement_db
\dt authorized_users
SELECT * FROM authorized_users;
```

### 4. Проверить в Telegram
- Откройте бота
- Должно загрузиться приложение
- Если есть в authorized_users - работает

---

## ⚙️ Значение переменных окружения

**Текущие (используются):**
- `POSTGRES_HOST` - хост БД
- `POSTGRES_PORT` - порт БД
- `POSTGRES_DB` - имя БД
- `POSTGRES_USER` - пользователь БД
- `DB_PASSWORD` - пароль БД

**Новые (на будущее):**
- `REQUIRE_AUTH=true` - включить/отключить авторизацию

---

## 📌 Важные замечания

1. **Таблица создается автоматически** - не нужно ничего делать вручную при первом запуске
2. **Telegram ID нужно узнать** - используйте бота @userinfobot
3. **Первый запуск** - покажет предупреждение что авторизованных пользователей нет
4. **Браузер заблокирован** - это нормально и безопасно
5. **Миграция встраивается** - из файла `05-add-telegram-auth.sql` при старте

---

## 🐛 Возможные проблемы

### Ошибка "не удается создать таблицу"
- Проверьте права пользователя БД
- Проверьте что БД запущена
- Смотрите логи: `docker logs procurement-db`

### "Telegram данные не найдены"
- Приложение не открыто в Telegram
- Обновите Telegram до последней версии
- Перезагрузите браузер

### "Telegram ID не авторизован"
- Проверьте что ID добавлен в БД
- Убедитесь что `is_active = true`
- Очистите localStorage и перезагрузите

---

## 📚 Полная документация

Смотрите:
- [AUTH_SETUP_GUIDE.md](AUTH_SETUP_GUIDE.md) - подробная инструкция по настройке
- [TELEGRAM_AUTH.md](TELEGRAM_AUTH.md) - описание системы авторизации

---

**Версия: 2.0**  
**Дата: 27 января 2026**  
**Статус: Production Ready** 🚀
