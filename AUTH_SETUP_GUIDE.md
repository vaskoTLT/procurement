# Авторизация через Telegram ID - Полная инструкция

## 🔒 Как работает безопасность

1. **Таблица `authorized_users`** - создается автоматически при первом запуске сервера
2. **Проверка источника запроса** - сервер отклоняет все запросы не из Telegram WebApp
3. **Проверка Telegram ID** - только пользователи из таблицы `authorized_users` имеют доступ
4. **Браузер заблокирован** - доступ лишь только через Telegram Mini App

## 🚀 Быстрый старт

### 1. Развернуть приложение

```bash
# Остановить (если работает)
./stop-project.sh

# Пересобрать
./rebuild-project.sh

# Запустить
./start-project.sh
```

При первом запуске бэкенд автоматически:
- ✅ Создаст таблицу `authorized_users`
- ✅ Создаст необходимые индексы
- ✅ Проверит наличие авторизованных пользователей

### 2. Добавить первого авторизованного пользователя

После развертывания вы увидите в логах бэкенда:
```
⚠️ ВНИМАНИЕ: Нет авторизованных пользователей!
⚠️ Добавьте первого пользователя используя SQL...
```

#### Способ A: Через Docker (рекомендуется)

```bash
# Подключиться к БД контейнеру
docker exec -it procurement-db psql -U procurement_user -d procurement_db

# Добавить пользователя (замените 123456789 на реальный Telegram ID)
INSERT INTO authorized_users (telegram_id, username) 
VALUES (123456789, 'admin');

# Проверить
SELECT * FROM authorized_users;

# Выход
\q
```

#### Способ B: Через локальный PostgreSQL клиент

```bash
# Если PostgreSQL установлен локально
psql -h 127.0.0.1 -p 5432 -U procurement_user -d procurement_db

# Остальные команды SQL как выше
```

### 3. Узнать свой Telegram ID

**Способ 1: Через @userinfobot (самый простой)**
1. Откройте в Telegram: https://t.me/userinfobot
2. Нажмите START
3. Скопируйте число из поля `Id` - это ваш Telegram ID

**Способ 2: Через браузерную консоль**
1. Откройте web.telegram.org
2. F12 → Console
3. Введите: `Telegram.User.id`

## 📋 Управление пользователями

### Добавить пользователя

```sql
INSERT INTO authorized_users (telegram_id, username, description) 
VALUES (123456789, 'ivan', 'Иван Петров');
```

### Добавить несколько пользователей сразу

```sql
INSERT INTO authorized_users (telegram_id, username, description) 
VALUES 
  (123456789, 'admin', 'Администратор'),
  (987654321, 'user1', 'Первый пользователь'),
  (555555555, 'user2', 'Второй пользователь');
```

### Посмотреть всех авторизованных

```sql
SELECT id, telegram_id, username, description, authorized_at, is_active 
FROM authorized_users 
ORDER BY authorized_at DESC;
```

### Запретить доступ пользователю

```sql
UPDATE authorized_users SET is_active = false WHERE telegram_id = 123456789;
```

### Разрешить доступ пользователю (после блокировки)

```sql
UPDATE authorized_users SET is_active = true WHERE telegram_id = 123456789;
```

### Удалить пользователя

```sql
DELETE FROM authorized_users WHERE telegram_id = 123456789;
```

## 🛡️ Защита от внешнего доступа

### Что блокируется?

- ❌ Доступ через браузер (`http://localhost:3000`, `http://example.com`)
- ❌ Запросы без заголовка `X-Telegram-Id`
- ❌ Запросы с неавторизованным Telegram ID
- ❌ Запросы не из Telegram WebApp контекста

### Как это работает?

Бэкенд проверяет:
1. **Заголовок `X-Telegram-WebApp`** - должен быть `true` (отправляется фронтенда)
2. **Наличие `X-Telegram-Id`** - Telegram ID пользователя
3. **Наличие в БД** - только если ID есть в таблице `authorized_users`

Ответ если доступ запрещен:
```json
{
  "success": false,
  "error": "UNAUTHORIZED",
  "message": "Доступ только из Telegram. Откройте приложение через бота в Telegram."
}
```

## 📊 Структура таблицы `authorized_users`

```sql
CREATE TABLE authorized_users (
  id SERIAL PRIMARY KEY,                    -- Внутренний ID
  telegram_id BIGINT UNIQUE NOT NULL,       -- Telegram ID (берется из Telegram)
  username VARCHAR(100),                    -- Имя пользователя
  description VARCHAR(255),                 -- Описание (опционально)
  is_active BOOLEAN DEFAULT true,           -- Активен ли пользователь
  authorized_at TIMESTAMP DEFAULT NOW(),    -- Когда добавлен
  authorized_by VARCHAR(100) DEFAULT 'system' -- Кто добавил
);
```

## 🔧 API endpoints авторизации

### GET /api/auth/check
Проверить авторизован ли пользователь

**Заголовки:**
```
X-Telegram-Id: 123456789
X-Telegram-WebApp: true
```

**Ответ (авторизован):**
```json
{
  "success": true,
  "authorized": true,
  "userId": 1
}
```

**Ответ (не авторизован):**
```json
{
  "success": false,
  "authorized": false,
  "error": "ACCESS_DENIED"
}
```

### GET /api/auth/me
Получить информацию о текущем пользователе

**Ответ:**
```json
{
  "success": true,
  "user": {
    "id": 1,
    "telegram_id": 123456789,
    "username": "admin",
    "authorized_at": "2024-01-15T10:30:00Z"
  }
}
```

## 📝 Миграция при запуске

При запуске бэкенда автоматически выполняется:

1. **Проверка таблицы `users`**
   - Добавляется поле `telegram_id` если его нет

2. **Проверка таблицы `authorized_users`**
   - Создается если не существует (из файла `init-db/05-add-telegram-auth.sql`)
   - Создаются индексы для быстрого поиска

3. **Проверка авторизованных пользователей**
   - Выводит предупреждение если их нет

## 📈 Логирование авторизации

### На фронтенде
```
🔐 Проверяем авторизацию...
✅ Пользователь авторизован. Telegram ID: 123456789
❌ Доступ запрещен. Ваш Telegram ID не авторизован.
```

Смотреть в браузере: F12 → Console

### На бэкенде
```bash
docker logs procurement-backend | grep -E "(✅|❌|⚠️)"
```

## ⚙️ Переменные окружения

На данный момент не требуются, так как все хранится в БД.

Если в будущем потребуется:
```bash
# REQUIRED_AUTH=true # Включить/отключить авторизацию
# ADMIN_ONLY=false   # Требовать ли admin права
```

## 🧪 Локальное тестирование

Для тестирования на локальной машине без Telegram:

**Способ 1: Временное изменение для разработки**
Отредактируйте `frontend/services/apiService.ts`:

```typescript
getTelegramData() {
  // Для локального тестирования
  if (window.location.hostname === 'localhost') {
    console.log('ℹ️ Используется тестовый Telegram ID для локального разработки');
    return { 
      id: 123456789,  // Ваш ID
      first_name: 'Dev',
      is_bot: false
    };
  }
  
  const tg = (window as any).Telegram?.WebApp;
  // ... остальной код
}
```

Не забудьте вернуть обратно перед деплоем!

## 🐛 Решение проблем

### "Доступ только из Telegram"
- ✓ Приложение должно открываться через Telegram Mini App
- ✓ Не открывайте прямой URL в браузере
- ✓ Откройте через бота в Telegram (@YourBotName)

### "Доступ запрещен. Telegram ID не авторизован"
- ✓ Проверьте что Telegram ID добавлен в таблицу `authorized_users`
- ✓ Проверьте что `is_active = true`
- ✓ Перезагрузите приложение
- ✓ Очистите localStorage: F12 → Application → Clear storage

### "Telegram данные не найдены"
- ✓ Приложение не открыто в контексте Telegram
- ✓ Обновите Telegram до последней версии
- ✓ Перезагрузите приложение

### БД не создается при старте
- ✓ Проверьте что конфигурация PostgreSQL правильная
- ✓ Проверьте логи: `docker logs procurement-backend | tail -50`
- ✓ Убедитесь что пользователь БД имеет права на создание таблиц

## 📞 Поддержка

Для вопросов обратитесь к администратору приложения.
