# Миграционный гайд: со старой конфигурации на Traefik

## 📋 Что изменилось

### До (старая конфигурация):
- Контейнеры слушали на прямых портах (80, 443, 3002)
- Nginx был в контейнере frontend для reverse proxy
- Нет автоматического управления SSL/HTTPS
- Портаж фиксирован на локальной машине

### После (Traefik конфигурация):
- Все контейнеры слушают только локально в своих сетях
- Traefik управляет HTTPS и маршрутизацией
- Автоматическая выдача Let's Encrypt сертификатов
- Гибкая маршрутизация по доменам и путям
- Легче масштабировать на несколько доменов/приложений

## 🔄 Пошаговая миграция

### Фаза 1: Подготовка (на локальной машине)

#### 1.1 Создать резервную копию

```bash
cd /media/media/my_projects/n8n/procurement/procurement

# Архивировать текущую конфигурацию
tar -czf backup-old-config.tar.gz docker-compose.yml backend/ frontend/ init-db/

# Сохранить в безопасном месте
mv backup-old-config.tar.gz ~/backups/
```

#### 1.2 Коммитить текущее состояние в Git

```bash
git add -A
git commit -m "Backup: Old configuration before Traefik migration"
git push origin main
```

#### 1.3 Обновить файлы (они уже обновлены в этом проекте)

**Файлы, которые изменились:**
- `docker-compose.yml` - полностью переписан для Traefik
- `backend/Dockerfile` - добавлен healthcheck
- `backend/src/server.js` - обновлен CORS и trust proxy
- `frontend/Dockerfile` - добавлен healthcheck
- `frontend/nginx.conf` - убран proxy к backend
- `.env.production.example` - новый файл для production

**Новые файлы:**
- `TRAEFIK_DEPLOYMENT.md` - инструкции развертывания
- `LOCAL_TRAEFIK_TESTING.md` - локальное тестирование
- `deploy-traefik.sh` - скрипт для автоматизации

#### 1.4 Тестировать локально

```bash
# Следуйте инструкциям в LOCAL_TRAEFIK_TESTING.md
# Это тестирование важно перед production!
```

### Фаза 2: Подготовка на сервере

#### 2.1 Убедитесь, что Traefik работает

```bash
# Подключитесь на сервер
ssh root@your-server-ip

# Проверьте Traefik
docker ps | grep traefik
docker logs traefik -n 20

# Проверьте сеть proxy
docker network ls | grep proxy
```

#### 2.2 Создайте каталог развертывания

```bash
mkdir -p /opt/docker/compose/procurement
cd /opt/docker/compose/procurement
```

#### 2.3 Загрузите новые файлы

**Вариант 1: Git clone**
```bash
cd /opt/docker/compose
git clone https://github.com/your-repo/procurement.git
cd procurement
```

**Вариант 2: Копирование файлов**
```bash
# С локальной машины
scp -r /media/media/my_projects/n8n/procurement/procurement/* \
  root@your-server-ip:/opt/docker/compose/procurement/
```

### Фаза 3: Остановка старого приложения

#### 3.1 (ЕСЛИ) старое приложение еще запущено на сервере

```bash
# Найти старые контейнеры
docker ps | grep -i "shoper\|smart-shopping"

# Остановить старые контейнеры
docker stop $(docker ps -q --filter "label=project=shoper")

# Или если они в docker-compose:
cd /path/to/old/project
docker-compose down

# Сохранить данные БД если нужны
docker exec shoper-db pg_dump -U shoper_user shoper_db > /backup/shoper_db_backup.sql
```

#### 3.2 Очистить порты (если нужно)

```bash
# Проверить, какие приложения слушают на портах 80, 443, 3002
lsof -i :80
lsof -i :443
lsof -i :3002

# Остановить конфликтующие приложения если есть
```

### Фаза 4: Развертывание новой конфигурации

#### 4.1 Подготовить .env

```bash
cd /opt/docker/compose/procurement

# Копируем пример
cp .env.production.example .env

# Отредактируем с production значениями
nano .env
```

**Важные переменные:**
```env
DOMAIN=procurement.fros-ty.com
POSTGRES_DB=procurement_db
POSTGRES_USER=procurement_user
DB_PASSWORD=ОЧЕНЬ_БЕЗОПАСНЫЙ_ПАРОЛЬ_ЗДЕСЬ
SSL_EMAIL=admin@fros-ty.com
GENERIC_TIMEZONE=Europe/Moscow
```

#### 4.2 Построить и запустить

**Вариант 1: Автоматизированный скрипт (рекомендуется)**
```bash
chmod +x deploy-traefik.sh
./deploy-traefik.sh
```

**Вариант 2: Ручной процесс**
```bash
# Построить образы
docker-compose build

# Запустить
docker-compose up -d

# Проверить логи
docker-compose logs -f
```

#### 4.3 Проверить здоровье сервисов

```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs backend
docker-compose logs frontend
docker-compose logs postgres

# Проверить подключение к Traefik
docker network inspect proxy | grep -A 3 procurement

# Проверить API
curl -k https://procurement.fros-ty.com/api/health
```

### Фаза 5: Миграция данных (если требуется)

#### 5.1 Если нужно сохранить старую БД

```bash
# Экспортировать данные из старой БД
docker exec shoper-db pg_dump -U shoper_user -d shoper_db > /tmp/old_data.sql

# Импортировать в новую БД
cat /tmp/old_data.sql | docker-compose exec -T postgres psql -U procurement_user -d procurement_db
```

#### 5.2 Инициализировать новую схему БД

```bash
# Автоматически запустится при старте контейнера
# Проверить, что таблицы созданы:
docker-compose exec postgres psql -U procurement_user -d procurement_db -c "\dt"
```

### Фаза 6: Проверка и валидация

#### 6.1 Проверить endpoint доступности

```bash
# Frontend
curl -I https://procurement.fros-ty.com/

# API
curl -I https://procurement.fros-ty.com/api/health

# Health checks
curl -s https://procurement.fros-ty.com/api/health | jq

# Backend health
curl -s https://procurement.fros-ty.com/health | jq
```

#### 6.2 Проверить SSL сертификат

```bash
# Посмотреть детали сертификата
echo | openssl s_client -servername procurement.fros-ty.com \
  -connect procurement.fros-ty.com:443 2>/dev/null | \
  openssl x509 -noout -dates -subject

# Убедиться, что это Let's Encrypt сертификат
```

#### 6.3 Проверить логирование

```bash
# Логи Traefik
docker logs traefik | tail -50

# Логи приложения
docker-compose logs --tail 50
```

#### 6.4 Проверить производительность

```bash
# Статус контейнеров
docker stats

# Использование памяти, CPU
docker-compose ps

# Соединения
netstat -an | grep ESTABLISHED | wc -l
```

### Фаза 7: Отродийные работы

#### 7.1 Обновить DNS (если нужно)

```bash
# Убедитесь, что DNS указывает на новый сервер
nslookup procurement.fros-ty.com

# Может потребоваться до 24 часов для полного распространения
```

#### 7.2 Обновить документацию

- [ ] Обновить README с новыми инструкциями
- [ ] Документировать новые переменные окружения
- [ ] Обновить инструкции для команды

#### 7.3 Создать резервную копию новой конфигурации

```bash
# Резервная копия рабочей конфигурации
tar -czf /backups/procurement-traefik-working.tar.gz \
  /opt/docker/compose/procurement/

# Резервная копия БД
docker-compose exec -T postgres pg_dump -U procurement_user -d procurement_db > \
  /backups/procurement_db_$(date +%Y%m%d_%H%M%S).sql
```

## 🔍 Диагностика проблем при миграции

### Проблема: Старое приложение все еще работает

```bash
# Найти старые image и контейнеры
docker images | grep -i shoper
docker ps -a | grep -i shoper

# Удалить старые контейнеры
docker rm -f $(docker ps -a | grep shoper | awk '{print $1}')

# Удалить старые образы
docker rmi $(docker images | grep shoper | awk '{print $3}')
```

### Проблема: Конфликт портов

```bash
# Проверить, кто слушает на портах
sudo netstat -tulpn | grep LISTEN

# Найти процесс
sudo lsof -i :80
sudo lsof -i :443

# Остановить процесс
kill -9 <PID>
```

### Проблема: Traefik не видит новые сервисы

```bash
# Перезагрузить Traefik
docker restart traefik

# Проверить, что сеть proxy верно настроена
docker network inspect proxy

# Убедиться, что контейнеры подключены к proxy
docker inspect procurement-frontend | grep -A 10 '"Networks"'
```

### Проблема: Данные БД потеряны

```bash
# Если есть резервная копия
cat /backup/shoper_db_backup.sql | \
  docker-compose exec -T postgres psql -U procurement_user -d procurement_db

# Если нет - БД инициализируется автоматически из init.sql
```

## ✅ Чеклист после миграции

- [ ] Frontend доступен по HTTPS с valid сертификатом
- [ ] API endpoints работают корректно
- [ ] CORS headers правильно установлены
- [ ] База данных инициализирована и содержит данные
- [ ] Все сервисы показывают healthy status
- [ ] Нет errors в логах
- [ ] Traefik dashboard показывает оба маршрута (frontend + backend)
- [ ] SSL сертификат автоматически обновляется
- [ ] Резервные копии данных созданы
- [ ] Документация обновлена

## 📝 Откаты (если что-то пошло не так)

### Откат на старую версию

```bash
# Восстановить старую конфигурацию
tar -xzf ~/backups/backup-old-config.tar.gz

# Восстановить старую БД (если нужна)
docker-compose -f old-compose.yml exec postgres \
  psql -U shoper_user -d shoper_db < /backup/shoper_db_backup.sql

# Перезагрузить старое приложение
docker-compose -f old-compose.yml up -d
```

## 📞 Поддержка

Если возникли проблемы при миграции:

1. Проверьте логи всех компонентов
2. Убедитесь в правильности .env файла
3. Перечитайте TRAEFIK_DEPLOYMENT.md
4. Проверьте LOCAL_TRAEFIK_TESTING.md для локального воспроизведения проблемы
5. Откатитесь на старую версию если нужно
6. Свяжитесь с поддержкой

---

**Важно:** Всегда тестируйте локально перед production миграцией!
