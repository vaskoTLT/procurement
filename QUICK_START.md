# ⚡ Быстрый старт (Quick Start)

## 🚀 За 3 минуты

### 1. Убедитесь что Docker установлен
```bash
docker --version
docker-compose --version
```

### 2. Перейдите в папку проекта
```bash
cd /media/media/my_projects/n8n/procurement/procurement
```

### 3. Запустите проект
```bash
docker-compose up -d
```

### 4. Дождитесь запуска (20-30 секунд)
```bash
# Проверить статус
docker-compose ps

# Или посмотреть логи
docker-compose logs -f
```

## ✅ Проверьте что всё работает

### В браузере:
- **Frontend**: http://localhost (или потребуется настройка через Traefik)
- **Backend API**: http://localhost:3002/api/lists

### В терминале:
```bash
# Проверить backend
curl http://localhost:3002/api/lists

# Должен вернуть:
# {"success":true,"lists":[...]}
```

## 📱 Основные действия

### Создать список
1. Нажмите **"+"** в нижней части экрана
2. Введите название списка
3. Нажмите **"Создать"**

### Добавить товар в список
1. Откройте список
2. Нажмите **"Добавить товар"**
3. Заполните:
   - Название (например: "Хлеб")
   - Количество (например: "10")
   - Единицу измерения (шт, кг, л и т.д.)
   - Цену (например: "100")
4. Нажмите **"Добавить"**

### Отметить товар как куплен
1. Нажмите на кружок слева от товара
2. Кружок станет зеленым с галочкой ✓

### Редактировать товар
1. Нажмите на значок ✏️ рядом с товаром
2. Отредактируйте поля
3. **Новое!** Добавьте подсписки покупок
4. Нажмите **"Сохранить"**

### Добавить подсписок (новое)
*(В окне редактирования товара)*
1. Раскройте раздел **"Подсписки покупок"**
2. Введите количество и цену
3. Нажмите **"Добавить подсписок"**
4. Товар автоматически помечается как куплено, если сумма подсписков ≥ запланированного количества

### Редактировать список
1. Откройте список
2. Нажмите на значок ✏️ в заголовке
3. Обновите название и описание
4. Нажмите **"Сохранить"**

### Удалить список
1. На главной странице нажмите на 🗑️ рядом со списком
2. Подтвердите удаление

## 🔄 Перезапустить проект

### Остановить
```bash
docker-compose down
```

### Остановить и удалить БД
```bash
docker-compose down -v
```

### Запустить заново
```bash
docker-compose up -d
```

## 🆘 Проблемы и решения

### Порт 3002 занят
```bash
# Найдите процесс
lsof -i :3002

# Или измените порт в docker-compose.yml
# backend:
#   ports:
#     - "3003:3002"  # Измените на 3003
```

### Контейнер не запускается
```bash
# Посмотрите полные логи
docker-compose logs backend
docker-compose logs postgres
docker-compose logs frontend

# Проверьте что в init-db правильные файлы
ls -la init-db/
```

### БД не инициализирована
```bash
# Удалите volume и перезапустите
docker-compose down -v
docker-compose up -d

# Если проблема повторится:
docker-compose exec postgres psql -U procurement_user -d procurement_db -c "\dt"
# Должны отобразиться таблицы
```

### Frontend не грузится
```bash
# Проверьте что backend доступен:
curl http://localhost:3002/api/lists

# Посмотрите консоль браузера (F12 → Console)
# обычно там видна ошибка подключения
```

## 📋 Файлы конфигурации

### Основные файлы проекта:
```
.
├── docker-compose.yml          # Конфигурация Docker
├── backend/
│   ├── Dockerfile
│   ├── package.json
│   └── src/
│       ├── server.js
│       ├── models/
│       │   ├── database.js     # Подключение к БД
│       │   └── ItemModel.js    # Модель товара (с подсписками)
│       └── routes/
│           ├── items.js        # API товаров (с подсписками)
│           └── lists.js        # API списков
├── frontend/
│   ├── Dockerfile
│   ├── package.json
│   ├── src/
│   │   ├── App.tsx
│   │   ├── types.ts            # TypeScript типы
│   │   ├── services/
│   │   │   └── apiService.ts   # API клиент (с подсписками)
│   │   └── components/
│   │       ├── HomeView.tsx
│   │       ├── ListDetailView.tsx
│   │       ├── ItemEditModal.tsx    # Новое!
│   │       └── ListEditModal.tsx    # Новое!
├── init-db/
│   ├── init.sql                # Основная инициализация
│   └── 02-update-schema.sql    # Новая схема (подсписки)
├── IMPLEMENTATION_GUIDE.md     # Полный гайд
└── API_DOCUMENTATION.md        # API документация
```

## 🎯 Что дальше?

- **Статистика**: Нажмите на иконку графика внизу, чтобы увидеть аналитику
- **Категории**: Товары автоматически сортируются по категориям
- **Печать**: Используйте встроеную функцию печати браузера (Ctrl+P)
- **Синхронизация**: Все изменения сохраняются в БД автоматически

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи: `docker-compose logs`
2. Перезагрузите проект: `docker-compose down && docker-compose up -d`
3. Откройте консоль браузера: F12 → Console/Network
4. Проверьте подключение к API: `curl http://localhost:3002/api/lists`
