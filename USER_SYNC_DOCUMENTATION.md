# 🔄 Документация по синхронизации пользователей

## 📋 Обзор

Этот документ описывает механизм автоматической синхронизации пользователей между базой данных **supabase-db** (таблица `public.n8n_registration`) и **procurement-db** (таблица `authorized_users`).

## 🎯 Цели синхронизации

1. **Автоматическое добавление** новых пользователей из supabase в procurement
2. **Гибридный подход**: синхронизация при запросе + периодическая проверка
3. **Отказоустойчивость**: работа даже при недоступности supabase-db
4. **Минимальная нагрузка**: оптимизированные запросы и кэширование

## 🔧 Как работает синхронизация

### 1. Гибридный механизм

**a) Синхронизация по требованию (On-Demand)**
- Вызывается при каждом запросе к `authorized_users` через `authMiddleware`
- Проверяет, прошло ли достаточно времени с последней синхронизации (1 минута)
- Если да, выполняет полную синхронизацию

**b) Периодическая синхронизация**
- Запускается автоматически при старте сервера
- Выполняется каждую минуту
- Гарантирует, что новые пользователи будут добавлены даже без активных запросов

### 2. Алгоритм синхронизации

1. **Подключение к supabase-db**
   - Использует отдельный пул соединений
   - Обрабатывает ошибки подключения (не прерывает основную работу)

2. **Получение данных из supabase**
   ```sql
   SELECT telegram_id FROM public.n8n_registration WHERE telegram_id IS NOT NULL
   ```

3. **Получение текущих данных из procurement**
   ```sql
   SELECT telegram_id FROM authorized_users
   ```

4. **Сравнение и добавление новых пользователей**
   - Находит разницу между двумя наборами данных
   - Добавляет только новые telegram_id
   - Не обновляет существующие записи

5. **Добавление новых пользователей**
   ```sql
   INSERT INTO authorized_users (telegram_id, username, description, is_active)
   VALUES ($1, $2, $3, $4)
   ON CONFLICT (telegram_id) DO NOTHING
   ```

### 3. Параметры синхронизации

- **Интервал**: 1 минута
- **Таймаут подключения**: 5 секунд
- **Максимальное количество соединений**: 5
- **Автоматическая активация**: `is_active = true`
- **Генерация username**: `user_${telegram_id}`

## 🚀 Использование

### Ручная синхронизация

```bash
# Выполнить ручную синхронизацию
curl -X POST "http://localhost:3002/api/sync-users" \
  -H "Content-Type: application/json"

# Ожидаемый ответ:
{
  "success": true,
  "message": "Синхронизация завершена"
}
```

### Проверка статуса синхронизации

```bash
# Просмотр логов синхронизации
docker compose logs -f backend | grep "🔄\|✅\|🆕"

# Просмотр текущих пользователей
docker exec -it procurement-db psql -U procurement_user -d procurement_db -c "SELECT * FROM authorized_users;"
```

## 📊 Пример работы

### Исходное состояние

**supabase-db (n8n_registration):**
```
 id | telegram_id
----+-------------
  4 | 610248913
  5 | 5000811741
```

**procurement-db (authorized_users):**
```
 id | telegram_id | username
----+-------------+----------
  1 | 123456789   | admin
  2 | 610248913   | admin
```

### После синхронизации

**procurement-db (authorized_users):**
```
 id | telegram_id | username    | description
----+-------------+-------------+---------------------
  1 | 123456789   | admin       | Default admin user
  2 | 610248913   | admin       | Main administrator
  3 | 5000811741  | user_5000811741 | Auto-sync from supabase
```

## 🛡️ Обработка ошибок

### 1. Supabase-db недоступна
- Логируется предупреждение
- Основная работа продолжается
- Следующая попытка через 1 минуту

### 2. Ошибки подключения
- Таймаут: 5 секунд
- Автоматическое повторное подключение
- Не прерывает работу основного API

### 3. Конфликты данных
- Используется `ON CONFLICT DO NOTHING`
- Существующие записи не обновляются
- Нет дубликатов

## 🔧 Настройка

### Параметры в коде

Файл: `backend/src/models/syncUsers.js`

```javascript
// Интервал синхронизации (минуты)
const SYNC_INTERVAL_MINUTES = 1;

// Конфигурация подключения к supabase
const supabaseConfig = {
  host: 'supabase-db',
  port: 5432,
  database: 'postgres',
  user: 'postgres',
  password: '',
  max: 5,
  idleTimeoutMillis: 10000,
  connectionTimeoutMillis: 5000,
};
```

### Docker конфигурация

Файл: `docker-compose.yml`

```yaml
backend:
  depends_on:
    postgres:
      condition: service_healthy
    supabase-db:
      condition: service_healthy
  networks:
    - procurement-internal
    - proxy
    - supabase-network
```

## 🎯 Преимущества решения

1. **Надежность**: Работает даже при недоступности supabase-db
2. **Производительность**: Минимальная нагрузка на базы данных
3. **Автоматизация**: Не требует ручного вмешательства
4. **Масштабируемость**: Легко изменять интервал синхронизации
5. **Отказоустойчивость**: Ошибки не прерывают основную работу

## 📚 Логирование

Все события синхронизации логируются с префиксами:

- `🔄` - Начало/конец синхронизации
- `✅` - Успешные операции
- `🆕` - Новые пользователи
- `⚠️` - Предупреждения
- `❌` - Ошибки

## 🔮 Будущие улучшения

1. **Конфигурируемый интервал** через переменные окружения
2. **Уведомления** о новых пользователях
3. **Логирование** в отдельную таблицу
4. **Веб-интерфейс** для управления синхронизацией

Теперь ваша система автоматически синхронизирует пользователей между базами данных! 🎉