# ✅ Чек-лист развертывания авторизации

## Перед развертыванием

- [ ] Обновлен весь код (`git pull`)
- [ ] Backup БД сделан (если есть важные данные)
- [ ] Готовы Telegram ID пользователей для добавления

## Развертывание

```bash
# 1. Остановить приложение
./stop-project.sh

# 2. Пересобрать
./rebuild-project.sh

# 3. Запустить
./start-project.sh

# 4. Подождать 30 секунд для инициализации
sleep 30

# 5. Проверить логи
docker logs procurement-backend | tail -50
```

## Проверка логов

Должны видеть эти сообщения в логах бэкенда:

```
✅ Подключение к PostgreSQL успешно
📊 Инициализация базы данных...
✅ Поле telegram_id в таблице users готово
⚡ Таблица authorized_users не найдена. Выполняем миграцию...
✅ Миграция авторизации выполнена успешно
⚠️ ВНИМАНИЕ: Нет авторизованных пользователей!
⚠️ Добавьте первого пользователя...
✅ Авторизованных пользователей: 0
✅ Таблицы в БД: authorized_users, items, list_participants, ...
🚀 Сервер запущен на порту 3002
```

## Добавление первого пользователя

```bash
# Подключиться к БД
docker exec -it procurement-db psql -U procurement_user -d procurement_db

# Добавить пользователя (замените 123456789)
INSERT INTO authorized_users (telegram_id, username) 
VALUES (123456789, 'admin');

# Проверить
SELECT * FROM authorized_users;

# Должен быть результат:
# id | telegram_id | username | is_active | ...
# 1  | 123456789   | admin    | t         | ...

# Выход
\q
```

## Проверка фронтенда

### В браузере (должно НЕ работать):
1. Откройте `http://localhost:3000`
2. Должно быть сообщение об ошибке:
   - "Доступ только из Telegram"
3. ✅ PASS - это нормально, так и должно быть

### В Telegram WebApp (должно работать):
1. Откройте бота в Telegram
2. Откройте Mini App
3. Должно либо:
   - Загрузиться приложение (если вы авторизованы)
   - Показать "Доступ запрещен" (если в БД вас нет)
4. ✅ PASS - авторизация работает

## Проверка БД

```bash
# Подключиться
docker exec -it procurement-db psql -U procurement_user -d procurement_db

# Проверить существование таблиц
\dt

# Должны быть:
# authorized_users - НОВАЯ, с авторизованными пользователями
# items
# list_participants
# shopping_lists
# users - (обновлена с добавлением telegram_id)
# item_purchases

# Проверить авторизованных пользователей
SELECT COUNT(*) FROM authorized_users;

# Проверить что users таблица имеет telegram_id
\d users

# Должно быть поле: telegram_id | bigint

# Выход
\q
```

## Проверка API endpoints

```bash
# Проверить auth/check БЕЗ Telegram ID (должно быть 401)
curl -X GET http://localhost:3002/api/auth/check

# Должен быть результат:
# {"success":false,"authorized":false,"error":"MISSING_AUTH"}

# ✅ PASS

# Проверить auth/check БЕЗ X-Telegram-WebApp (должно быть 403)
curl -X GET http://localhost:3002/api/auth/check \
  -H "X-Telegram-Id: 123456789"

# Должен быть результат:
# {"success":false,"error":"UNAUTHORIZED","message":"Доступ только из Telegram WebApp"}

# ✅ PASS

# Проверить auth/check С заголовками (после добавления пользователя)
curl -X GET http://localhost:3002/api/auth/check \
  -H "X-Telegram-Id: 123456789" \
  -H "X-Telegram-WebApp: true"

# Должен быть результат:
# {"success":true,"authorized":true,"userId":1}

# ✅ PASS
```

## Проверка операций в приложении

После успешной авторизации через Telegram:

- [ ] Может создать список
- [ ] Может добавить товар  
- [ ] Может отметить товар как куплен
- [ ] Может удалить список
- [ ] Видит своих списков первым

## Проверка безопасности

### Браузер заблокирован? ✅
```bash
# В браузере откройте любой endpoint
curl http://localhost:3002/api/lists

# Должно быть:
# {"success":false,"error":"UNAUTHORIZED","message":"Доступ только из Telegram..."}
```

### Свои списки? ✅
```bash
# Два разных пользователя не должны видеть списки друг друга
# Проверьте что каждый видит только его списки
```

### Новый пользователь создается? ✅
```bash
# После авторизации нового telegram_id:
docker exec -it procurement-db psql -U procurement_user -d procurement_db

SELECT * FROM users WHERE telegram_id = 123456789;

# Должна появиться строка с username и telegram_id
```

## Итоговая проверка

**Все должно быть:**

- ✅ Таблица `authorized_users` создана
- ✅ Таблица `users` имеет `telegram_id`
- ✅ Авторизованные пользователи добавлены
- ✅ Браузер заблокирован
- ✅ Telegram WebApp работает
- ✅ Бэкенд логирует авторизацию
- ✅ API endpoints требуют авторизацию
- ✅ Каждый видит только свои списки

## Если что-то не работает

### Логи
```bash
# Бэкенд логи
docker logs procurement-backend | tail -100

# БД логи
docker logs procurement-db | tail -100

# Фронтенд логи (в браузере)
F12 → Console
```

### Перезагрузка
```bash
# Мягкая перезагрузка
docker-compose -f docker-compose.yml restart

# Жесткая перезагрузка
./stop-project.sh
./start-project.sh
```

### Полная очистка (осторожно!)
```bash
# ⚠️ ВНИМАНИЕ: это удалит все данные
./stop-project.sh
docker-compose -f docker-compose.yml down -v
./start-project.sh
# Потом добавить авторизованных пользователей
```

## Документация

После успешного развертывания:
1. Прочитайте [AUTH_SETUP_GUIDE.md](AUTH_SETUP_GUIDE.md) для полной документации
2. Прочитайте [CHANGELOG_AUTH.md](CHANGELOG_AUTH.md) для деталей изменений
3. Сохраните эту инструкцию для будущих развертываний

---

**Статус: ✅ Готово к развертыванию**  
**Дата: 27 января 2026**
